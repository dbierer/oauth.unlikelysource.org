<h1><?= $this->action ?> Authentication Result<h1>
<?php
$output = '<pre>Data Dump' . "\n";
$output .= var_export($this->result, TRUE);
$output .= '</pre>' . "\n";
$safe = '    ["token"] => string(129) "xxxxxxxxxx &lt;real token removed&gt; xxxxxxxxxx"';
$dump = explode("\n", $output);
foreach ($dump as $line) {
    echo (strpos($line, '[&quot;token&quot;]')) ? $safe : $line;
    echo PHP_EOL;
}
?>
